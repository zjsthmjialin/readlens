# 截图 / 预览资源

README「效果预览」用到的图：

- `vault-home.svg` —— 知识库「📖 首页」的**结构示意图**（我用 SVG 画的，非真实截图）。
- `report-category.png` / `report-daily-trend.png` —— `readlens report` 生成的**真实图表**。

欢迎补一张**真实 Obsidian 截图**替换或补充示意图,更直观:
`readlens quickstart --with-manual` → 用 Obsidian 打开 `./ReadLensDemo`、启用 Dataview → 截「📖 首页」即可，
命名为 `vault-home-real.png` 放这里，并在 README 里替换引用。
