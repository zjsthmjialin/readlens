# ReadLens 功能总表（待确认）

> 本表是 ReadLens 待做功能的**单一事实来源**。定位：**分发给别人用 —— 安装方便、上手容易、功能丰富**。
> 每项含：目的 / 交付内容 / 验收标准 / 优先级 / 状态。所有新功能都遵循 `CLAUDE.md`：
> **保持离线可跑（mock + offline AI）+ 补 pytest + 更新 skills/*.md**。
>
> 状态图例：✅ 已完成 · ⬜ 待做 · 🔜 已排期 · 💡 想法池
> 优先级：P0 立即 / P1 近期 / P2 中期 / P3 远期
>
> **请在本表上确认要做哪些、按什么优先级；确认后我再逐项推进。**

---

## 已完成（v0.2.0，供参照）
- ✅ 五大能力：平台适配层 / 多格式导出 / 读书报告 / AI 增值 / Obsidian 知识库
- ✅ vault 增量更新（保护手写内容与手填字段）
- ✅ 书籍笔记内嵌封面图
- ✅ DataviewJS 可视化统计页（评分分布 / 分类占比 / 各年读完）
- ✅ pip 包分发 + `readlens quickstart` 一键上手 + GitHub Actions CI + **已发布上线 PyPI**（v0.2.0）
- ✅ 阅读热力日历（B2）/ 统计快照与趋势（C4）/ 示例藏书随包（A3）—— v0.3.0

---

## A. 分发收尾（让「别人能装、能用、信得过」）

### A1 · 正式发布到 PyPI 🔜 P0（发布就绪，等上传）
- **目的**：真正做到 `pip install readlens`（当前只能从源码装）。
- **交付**：✅ `python -m build` 出 wheel/sdist（已通过 `twine check` + 干净环境装 wheel 跑通 quickstart）；
  ✅ 发布手册 `docs/RELEASE.md`（TestPyPI→正式→git tag 流程）。⬜ 实际 `twine upload`（需你的 PyPI Token）。
- **验收**：干净环境 `pip install readlens` 能装上并 `readlens quickstart` 跑通。
- **前置**：需 PyPI 账号 + 确认包名 `readlens` 可用；✅ 仓库 URL 已填 `zjsthmjialin`。

### A2 · 完善 README / 使用文档与徽章 ✅ P1
- **目的**：上手容易、专业可信。
- **交付**：✅ 安装/quickstart/接真实数据/FAQ；✅ CI/PyPI/Python/License 徽章；
  ✅ 效果预览区 + `docs/images/` 截图占位（截图待你补一张，见该目录说明）。
- **验收**：新用户仅看 README 即可 5 分钟内产出一个知识库。

### A3 · 示例数据随包可用 ✅ P2
- **目的**：pip 安装用户也能体验 `--manual` 手动藏书示例。
- **交付**：✅ `readlens/sampledata.py` 内置示例藏书；✅ `readlens quickstart --with-manual` 纳入演示，无需外部文件。
- **验收**：✅ 无源码时也能演示手动藏书入库（含 owned=none 触发购书清单）。

---

## B. Phase 1 收尾 · 打磨知识库

### B1 · 愿望清单 → 购书清单 ✅ P1
- **目的**：把「想读且未拥有」的书聚合成可执行的购书清单。
- **交付**：新增 `owned=none` 聚合的仪表盘页「购书清单」，支持 `priority` 字段（高/中/低）排序；
  frontmatter 增加 `priority` 与（可选）`price_target`；首页加入口。
- **验收**：demo 中 `owned=none` 的书出现在购书清单并可按优先级排序；补测试。

### B2 · 阅读热力图（日历视图）✅ P2
- **目的**：可视化统计更「好看」。
- **交付**：✅ 可视化统计页增加基于 `finished` 的**年×月热力网格**（DataviewJS，方块深浅 + 数字），无数据友好提示、容错。
- **验收**：✅ 有日期数据时渲染热力，无数据不报错；✅ 有测试。

---

## C. Phase 2 · 数据更丰富

### C1 · 豆瓣元数据增强 ✅ P1
- **目的**：补全 ISBN / 封面 / 出版社 / 出版年，让笔记更完整、封面更好看。
- **交付**：✅ 可插拔 `readlens/enrich/`（base + mock + douban）；✅ `readlens enrich` 预览、
  `readlens vault --enrich` 落库；✅ 只填空不覆盖；✅ 豆瓣带缓存与失败降级。见 `skills/enrich.md`。
- **验收**：✅ 缺 ISBN/出版信息的书增强后 frontmatter 补齐；✅ 离线（mock）可跑；✅ 3 个测试。
- **备注**：豆瓣仅补 cover/pubdate（ISBN 详情页不稳定未抓）；Model 新增 `Book.isbn/pubdate` 字段。

### C2 · weread 适配器真实 Key 联调 ✅ P1
- **目的**：让微信读书这条主数据源真正可用。
- **交付**：✅ 依官方 2026 skills 文档修正网关/字段/进度/书架映射（v0.4.0）；✅ 离线映射测试；
  ✅ `weread-check` 诊断命令 + `_call` 清晰报错；✅ **已用真实 Key 线上联调通过**。
- **验收**：✅ 真实账号 search/shelf/notebooks/笔记/统计均拉通并正确落库。

### C3 · 微信读书写操作 ⬜ P2
- **目的**：把 `create_thought` / `add_to_shelf` 从预留接口做成真实实现。
- **交付**：在 weread 适配器实现写操作 + CLI 子命令；离线用 mock 模拟。
- **验收**：能把一条想法写回/把一本书加入书架（或 mock 下模拟成功）；补测试。

### C4 · 阅读统计快照存档与趋势 ✅ P2
- **目的**：每次导入落一份带日期的统计，支持趋势对比。
- **交付**：✅ `06-统计快照/history.json` 按日期 upsert 持久累积；✅ `趋势.md` 展示历次与「与上期对比(±)」；
  ✅ `readlens vault --no-snapshot` 可关。见 `readlens/vault/snapshot.py`。
- **验收**：✅ 连续导入生成对比、同日不重复；✅ 有测试。

---

## D. Phase 3 · 分发与自动化（进阶）

### D1 · 定时任务：周期导入 + 周报/月报 ⬜ P2
- **目的**：把知识库变「活」——自动更新并推送阶段报告。
- **交付**：调度脚本（cron / GitHub Actions 定时 / Cowork 定时任务），周期性增量更新 vault + 生成周/月报。
- **验收**：按设定周期自动产出更新与报告。
- **前置**：C2（真实 Key）+ 一个运行环境。

### D2 · Skill 包（npx skills add 形态）⬜ P3
- **目的**：回归「脱胎于 WeChatReading Skills」，对 Agent 生态分发。
- **交付**：按目标 Skill 体系打包 + 清单。
- **验收**：可在支持的 Agent 里一键安装调用。

### D3 · 更多平台适配器 ⬜ P3
- **目的**：覆盖更多读者。
- **交付**：Kindle 标注 / Readwise / 微信收藏 适配器（按需逐个）。
- **验收**：每个新平台字段映射到统一模型，上层零改动；补测试。

---

## E. 智能增强（想法池 · 未排期）
- 💡 E1 划线全文检索（本地向量库）+ 跨书语义问答 · P3
- 💡 E2 读书笔记 AI 自动打标签（主题标注写入 tags）· P3
- 💡 E3 与日历/日记联动的阅读打卡 · P3
- 💡 E4 多用户 / 多 vault 配置 · P3

---

## 建议的首批推进顺序（供确认）
围绕「分发给别人用 + 功能丰富」，建议先做这批：
1. **A1 PyPI 发布** —— 让「pip 安装」真正成立（分发的临门一脚）。
2. **B1 购书清单** —— 低成本、体验加分、纯离线可做。
3. **A2 文档/截图/徽章** —— 上手体验与可信度。
4. **C1 豆瓣元数据增强** —— 让封面/ISBN 更丰富（配合已做的封面功能）。

其余（C2/C3/C4、D、E）按你意愿排。**请确认：这批顺序 OK 吗？是否有要加/删/改优先级的？**
